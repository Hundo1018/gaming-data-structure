# reference

Not a hypothesis. This is the oracle. It is measured on the same terms as every
candidate so that the cost of the obvious implementation is on the record, and
so that "faster than a hash map" is never mistaken for a result.

Verifying it compares it against itself and proves nothing.
